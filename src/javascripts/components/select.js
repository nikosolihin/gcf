export default class Select {
  constructor(el) {
    this.$el = $(el)

    // States
    this.active = 'Select--expand'

    // this.attachEvents()
  }

  attachEvents() {
    // Direct Events
    
  }
}
